# Copyright (c) 2023, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

# Contains certificates to be used by OpenSearch cluster
# Certificate name and secret name are kept as common and will be generating based on OpenSearchClusterSpec.name variable.

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.OpenSearchClusterSpec.name }}-admin-cert
  namespace: {{ .Release.Namespace }}
spec:
  commonName: admin
  secretName: {{ .Values.OpenSearchClusterSpec.name }}-admin-cert
  privateKey:
    algorithm: RSA
    encoding: PKCS8
    size: 2048
  duration: 2160h0m0s
  renewBefore: 360h0m0s
  subject:
    organizations:
      - verrazzano
  usages:
    - server auth
    - client auth
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: verrazzano-cluster-issuer
status: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.OpenSearchClusterSpec.name }}-dashboards-cert
  namespace: {{ .Release.Namespace }}
spec:
  commonName: {{ .Values.OpenSearchClusterSpec.name }}
  secretName: {{ .Values.OpenSearchClusterSpec.name }}-dashboards-cert
  dnsNames:
    - {{ .Values.OpenSearchClusterSpec.name }}
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}.svc
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}.svc.cluster.local
  privateKey:
    algorithm: RSA
    encoding: PKCS8
    size: 2048
  renewBefore: 360h0m0s
  duration: 2160h0m0s
  subject:
    organizations:
      - verrazzano
  usages:
    - server auth
    - client auth
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: verrazzano-cluster-issuer
status: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.OpenSearchClusterSpec.name }}-master-cert
  namespace: {{ .Release.Namespace }}
spec:
  commonName: {{ .Values.OpenSearchClusterSpec.name }}
  secretName: {{ .Values.OpenSearchClusterSpec.name }}-master-cert
  dnsNames:
    - {{ .Values.OpenSearchClusterSpec.name }}
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}.svc
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}.svc.cluster.local
    - {{ .Values.OpenSearchClusterSpec.name }}-discovery
  privateKey:
    algorithm: RSA
    encoding: PKCS8
    size: 2048
  renewBefore: 360h0m0s
  duration: 2160h0m0s
  subject:
    organizations:
      - verrazzano
  usages:
    - server auth
    - client auth
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: verrazzano-cluster-issuer
status: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.OpenSearchClusterSpec.name }}-node-cert
  namespace: {{ .Release.Namespace }}
spec:
  commonName: {{ .Values.OpenSearchClusterSpec.name }}
  secretName: {{ .Values.OpenSearchClusterSpec.name }}-node-cert
  dnsNames:
    - {{ .Values.OpenSearchClusterSpec.name }}
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}.svc
    - {{ .Values.OpenSearchClusterSpec.name }}.{{ .Release.Namespace }}.svc.cluster.local
  privateKey:
    algorithm: RSA
    encoding: PKCS8
    size: 2048
  renewBefore: 360h0m0s
  duration: 2160h0m0s
  subject:
    organizations:
      - verrazzano
  usages:
    - server auth
    - client auth
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: verrazzano-cluster-issuer
status: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.OpenSearchClusterSpec.name }}-monitor-certs
  namespace: verrazzano-monitoring
spec:
  commonName: prometheus-operator
  secretName: {{ .Values.OpenSearchClusterSpec.name }}-monitor-certs
  privateKey:
    algorithm: RSA
    encoding: PKCS8
    size: 2048
  renewBefore: 360h0m0s
  duration: 2160h0m0s
  subject:
    organizations:
      - verrazzano
  usages:
    - server auth
    - client auth
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: verrazzano-cluster-issuer
status: {}