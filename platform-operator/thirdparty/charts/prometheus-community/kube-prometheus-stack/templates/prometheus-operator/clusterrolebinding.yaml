{{- if and .Values.prometheusOperator.enabled .Values.global.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ template "kube-prometheus-stack.fullname" . }}-wls
  labels:
    app: {{ template "kube-prometheus-stack.name" . }}-wls
{{ include "kube-prometheus-stack.labels" . | indent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ template "kube-prometheus-stack.fullname" . }}-wls
subjects:
- kind: ServiceAccount
  name: {{ template "kube-prometheus-stack.wls.serviceAccountName" . }}
  namespace: {{ template "kube-prometheus-stack.namespace" . }}
{{- end }}
