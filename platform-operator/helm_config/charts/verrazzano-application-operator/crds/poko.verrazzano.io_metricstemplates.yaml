# Copyright (c) 2021, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: metricstemplates.poko.verrazzano.io
spec:
  group: poko.verrazzano.io
  names:
    kind: MetricsTemplate
    listKind: MetricsTemplateList
    plural: metricstemplates
    singular: metricstemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MetricsTemplate specifies the metrics template API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MetricsTemplateSpec specifies the desired state of a metrics
              template.
            properties:
              prometheusConfig:
                description: PrometheusConfig refers to the templated metrics scraping
                  configuration
                properties:
                  scrapeConfigTemplate:
                    description: ScrapeConfigTemplate is the prometheus scrape target
                      template to be added to the Prometheus Configmap
                    type: string
                  targetConfigMap:
                    description: TargetConfigMap contains metadata about the Prometheus
                      ConfigMap
                    properties:
                      namespace:
                        description: Namespace containing the Prometheus ConfigMap
                        type: string
                      port:
                        description: Name of the Prometheus ConfigMap
                        type: string
                    type: object
                type: object
              workloadSelector:
                description: TargetWorkload identifies the workloads to which this
                  template applies.
                properties:
                  priority:
                    description: Allows for explicit control of template selection.
                      Workloads with highest priority selected
                    type: integer
                  selector:
                    description: The label selector used to match the workload
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: A label selector requirement is a selector
                            that contains values, a key, and an operator that relates
                            the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: operator represents a key's relationship
                                to a set of values. Valid operators are In, NotIn,
                                Exists and DoesNotExist.
                              type: string
                            values:
                              description: values is an array of string values. If
                                the operator is In or NotIn, the values array must
                                be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced
                                during a strategic merge patch.
                              items:
                                type: string
                              type: array
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: matchLabels is a map of {key,value} pairs. A
                          single {key,value} in the matchLabels map is equivalent
                          to an element of matchExpressions, whose key field is "key",
                          the operator is "In", and the values array contains only
                          "value". The requirements are ANDed.
                        type: object
                    type: object
                type: object
            type: object
          status:
            description: MetricsTemplateStatus defines the observed state of MetricsTemplate
              and related resources.
            properties:
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              resources:
                description: Related resources affected by this metrics template
                items:
                  description: QualifiedResourceRelation identifies a specific related
                    resource (both APIVersion/Kind and namespace name) along this
                    the role of the resource in the relationship.
                  properties:
                    apiversion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    role:
                      type: string
                  required:
                  - apiversion
                  - kind
                  - name
                  - namespace
                  - role
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
