# Copyright (C) 2023, Oracle and/or its affiliates.
# Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.
apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: {{ .Values.name }}-webhook
  labels:
    app: {{ .Values.name }}-webhook
webhooks:
  - name: ocneociquickcreate.verrazzano.io
    clientConfig:
      service:
        name: {{ .Values.name }}-webhook
        namespace: {{ .Values.namespace }}
        path: "/mutate-clusters-verrazzano-io-v1alpha1-ocneociquickcreate"
    rules:
      - apiGroups:
          - clusters.verrazzano.io
        apiVersions:
          - v1
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - ocneociquickcreates
    sideEffects: None
    failurePolicy: Fail
    matchPolicy: Exact
    timeoutSeconds: 30
    admissionReviewVersions:
      - v1
      - v1alpha1
  - name: okequickcreate.verrazzano.io
    clientConfig:
      service:
        name: {{ .Values.name }}-webhook
        namespace: {{ .Values.namespace }}
        path: "/mutate-clusters-verrazzano-io-v1alpha1-okequickcreate"
    rules:
      - apiGroups:
          - clusters.verrazzano.io
        apiVersions:
          - v1
          - v1alpha1
        operations:
          - CREATE
          - UPDATE
        resources:
          - okequickcreates
    sideEffects: None
    failurePolicy: Fail
    matchPolicy: Exact
    timeoutSeconds: 30
    admissionReviewVersions:
      - v1
      - v1alpha1
