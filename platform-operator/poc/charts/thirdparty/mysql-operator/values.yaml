image:
  registry: ghcr.io
  repository: verrazzano
  tag: 8.0.32-2.0.8
  name: mysql-operator
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  pullSecrets:
    enabled: false
    secretName:

envs:
  imagesPullPolicy: IfNotPresent
  imagesDefaultRegistry: ghcr.io
  imagesDefaultRepository: verrazzano
