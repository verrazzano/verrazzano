// Copyright (c) 2022, Oracle and/or its affiliates.
// Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.

package install

/*
// TestInstallCmd - check that command reports not implemented yet
func TestInstallCmd(t *testing.T) {

	// Send the command output to a byte buffer
	buf := new(bytes.Buffer)
	errBuf := new(bytes.Buffer)
	rc := helpers.NewFakeRootCmdContext(genericclioptions.IOStreams{In: os.Stdin, Out: buf, ErrOut: errBuf})
	installCmd := NewCmdInstall(rc)
	assert.NotNil(t, installCmd)

	// Run install command, check for the expected status results to be displayed
	err := installCmd.Execute()
	assert.NoError(t, err)
	result := buf.String()
	assert.Equal(t, "Not implemented yet\n", result)
}
*/
